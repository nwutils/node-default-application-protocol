const helpers = require('./helpers.js');

const removeDefault = function (options) {
  helpers.throwError(options, 'This removeDefault API is not finished yet.');

  if (!options) {
    return;
  }

  return;
};

module.exports = removeDefault;
