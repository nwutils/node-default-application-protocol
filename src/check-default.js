const helpers = require('./helpers.js');

const checkDefaults = function (options) {
  helpers.throwError(options, 'The checkDefaults API is not finished yet.');

  if (!options) {
    return;
  }

  return;
};

module.exports = checkDefaults;
