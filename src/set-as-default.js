const helpers = require('./helpers.js');

const setAsDefault = function (options) {
  helpers.throwError(options, 'This setAsDefault API is not finished yet.');

  if (!options) {
    return;
  }

  return;
};

module.exports = setAsDefault;
