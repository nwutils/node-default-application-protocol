const helpers = require('./helpers.js');

const isDefault = function (options) {
  helpers.throwError(options, 'This isDefaults API is not finished yet.');

  if (!options) {
    return;
  }

  return;
};

module.exports = isDefault;
